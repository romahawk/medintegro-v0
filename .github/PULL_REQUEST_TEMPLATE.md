## What

<!-- One sentence: what does this PR do? -->

## Why

<!-- Why is this change necessary? Link to the issue it closes. -->

Closes #

## How

<!-- How was it implemented? Key decisions made here. -->

## How to Test

<!-- Step-by-step: how do I verify this works? -->
1.
2.
3.

## Checklist

- [ ] `pnpm build` exits 0 (no TypeScript errors)
- [ ] `pnpm lint` passes with no errors
- [ ] Tested in both EN and UA locales (if UI change)
- [ ] Tested in both dark and light themes (if UI change)
- [ ] Tested on mobile viewport (if UI change)
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] Acceptance criteria in the linked issue are all met
- [ ] No new `any` types, `@ts-ignore`, or `console.log` left in code
- [ ] No hardcoded user-facing strings (all text uses `t("key")`)

## Demo Artifact

<!-- Screenshot or Loom link showing the change working. Required for UI changes. -->

## Notes

<!-- Anything reviewers should know: tradeoffs, follow-up work, known limitations -->
